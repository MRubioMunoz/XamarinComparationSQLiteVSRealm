<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:viewmodels="clr-namespace:Comparacion.ViewModel"
             x:Class="Comparacion.View.CreateView">

    <ContentPage.BindingContext>
        <viewmodels:CreateViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <Button Text="Crear Todo" Command="{Binding CreateDates}" HorizontalOptions="CenterAndExpand"/>
            <Label Text="REALM" HorizontalOptions="CenterAndExpand"/>
            <Label Text="{Binding TiempoRealm}" HorizontalOptions="CenterAndExpand"/>
            <Label Text="SQLite" HorizontalOptions="CenterAndExpand"/>
            <Label Text="{Binding TiempoSQLite}" HorizontalOptions="CenterAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>